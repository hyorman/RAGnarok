{
  "name": "@ragnarok/vscode",
  "version": "0.4.0",
  "description": "VS Code extension for RAGnarōk — wires @ragnarok/core to VS Code APIs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p ./",
    "lint": "eslint src test test-harness",
    "lint:fix": "eslint --fix src test test-harness",
    "clean": "rimraf dist tsconfig.tsbuildinfo",
    "test": "echo 'VS Code extension tests must be run from root: npm test'"
  },
  "dependencies": {
    "@ragnarok/core": "0.4.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.105.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.3"
  }
}
