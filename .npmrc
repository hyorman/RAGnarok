# Enable cross-platform support for sharp and other native modules
# See: https://sharp.pixelplumbing.com/install/#cross-platform
foreground-scripts=true
install-strategy=shallow
# ext-apps requires the v1 SDK peer for its browser App bundle; the MCP server
# runtime uses v2 split packages. React peers are unused by the vanilla bundle.
legacy-peer-deps=true
